{
  "version": 2,
  "buildCommand": "cd frontend && npm install --legacy-peer-deps && npm run build && rm -rf node_modules",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}